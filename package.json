{
  "name": "chrome-mcp",
  "version": "1.0.0",
  "description": "MCP server for persistent multi-profile Chrome automation",
  "type": "module",
  "private": true,
  "license": "MIT",
  "main": "src/server.js",
  "scripts": {
    "check": "node --check src/server.js && bash -n setup-chrome-mcp.sh setup-novnc.sh && npm test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "puppeteer-core": "^24.43.1",
    "zod": "^4.3.6"
  }
}
